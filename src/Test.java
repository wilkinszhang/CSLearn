import java.io.*;
import java.util.*;

public class Test {
    String name;
    String id;

    void display() {
        System.out.println("Name: " + name + " ID: " + id);
    }

    public static void main(String[] args) {
        System.out.println(0b11 + (017 & 0x1A));
        Thread thread=new Thread(()->{

        });

        ThreadLocal threadLocal=new ThreadLocal<>();
        threadLocal.remove();
    }
}