# 多多在玩一个传送门游戏。游戏开始时少少在一维数轴的x=0处。他有n个传送门，每个传送门都有一个传送值ai，他能使用该传送门从x=t位置传送到x=t+ai，传送
# 门是消耗品，只能使用一次。同时他还有一个"反转"技能，使用该技能可以立即从位置 x=t"反转"到x=-t。少少可以以任意顺序使用这些传送门，可以在任何时候使
# 用"反转"技能(最多用一次，也可以不用)，问用完所有传送门后，少少到初始位置x=0最远的距离为多少?输入描述第一行为一个正整数 n(1 ≤ n ≤ 10^5)第二行为
# n个整数a1,a2,...,an(-10^4 < ai ≤ 10^4)输出描述输出用完所有传送门后，少少到初始位置距离的最大值。补充说明对于 60% 的数据，1 <= n <= 10对
# 于 100%的数据，1 <= n <= 10^5,-10^4 <= ai <= 10^4示例1输入4
# 1 -2 3 -4
# 输出10
# 最初少少在位置x=0处;他先选择使用第 2,4个传送门，到达位置x=0+a2+a4=0-2-4=-6，然后他使用技能“反转”，到达位置x=6，最后选择第 1,3 个
# 传送门，到达位置,x=6+a1+a3=6+1+3=10，与初始位置距离最大为10。
# 读取输入
n = int(input().strip())
a = list(map(int, input().split()))

# 计算所有传送门的绝对值之和
result = sum(abs(x) for x in a)

# 输出结果
print(result)
